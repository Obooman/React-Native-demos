# React-Native-demos

To run demos, never forget to npm install depedencies.

like this:

```bash
$ cd path/to/NavigatorFrame
$ npm install
```

without action above Xcode will though out an error 
`can't find file RootView.h`

Some of these demos are based on react-native 0.17,pay attention to the `package.json` depedencies version.